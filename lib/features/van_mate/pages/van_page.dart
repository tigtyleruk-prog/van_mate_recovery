import 'package:flutter/material.dart';

import 'van_firebase_page.dart';

class VanPage extends StatelessWidget {
  const VanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const VanFirebasePage();
  }
}
