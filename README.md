# Van Mate Standalone App

This directory contains the standalone Van Mate Flutter app.

The copied `lib/features/van_mate` feature remains the source of truth for the
Van Mate experience, while app-level bootstrap, shell, theme, and workspace
preparation live under `lib/app`.
